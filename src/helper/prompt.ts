export const prompt = (
  type: string,
  description: string,
  name: string,
  category: string,
) => {
  return `buatkan copywrting khusus untuk ${type} untuk produk ${name} dengan kategory ${category} dan deskripsi produk  ${description} buatkan dengan berbagai angle teknik copywriting seperti AIDA,PAS,FOMO,Social Proof, Story Telling, Urgency  dan semua itu harus menggunakan  bahasa yang catchy dan menarik`;
};
